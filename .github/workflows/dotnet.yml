name:  Build & Test - .NET API

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: Build and Test .NET Project
    runs-on: ubuntu-latest
    defaults:
        run:
          working-directory: ./BackEnd

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup .NET 9
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      - name: Restore dependencies
        run: dotnet restore MarketplaceArtesanato.sln

      - name: Build
        run: dotnet build MarketplaceArtesanato.sln --configuration Release --no-restore

      - name: Run Tests
        run: dotnet test MarketplaceArtesanato.Tests/MarketplaceArtesanato.Tests.csproj --no-build --verbosity normal

      - name: Publish API
        run: dotnet publish MarketplaceArtesanato.API/MarketplaceArtesanato.API.csproj -c Release -o ./publish



      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: MarketplaceArtesanato-API
          path: ./publish
